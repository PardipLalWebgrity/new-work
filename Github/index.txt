git config --global core.editor "code --wait" = Default Text editor VS Code
git config --global core.editor = Check default text editor
git --version = Check if Git is in PATH
git config --local user.name "Your Name" = name will be attached to your commits
git config --global user.name "Your Name" = name will be attached to your commits
git config --system user.name "Your Name" = name will be attached to your commits
git config --global user.email "you@example.com" = email is also attached to your commits
git config --global user.name = Check global Git username
git config --global user.email = Check global email
git config --list = See all Git config settings
git config --global init.defaultBranch main = initial branch name all newly created repositories.
git init = create a new Git repository
git status = Check which files are tracked
git add <file> = To add a file to the staging area
git add --all or git add -A = Stage all changes
git status - See what is staged, in which branch
git restore --staged <file> = bring back the last committed version of file
git reset <file> = remove file from staging
git commit -m "message" - Commit staged changes with a message
git commit -a -m "message" = stages all modified and deleted files.Then commits with message.
git log = See commit history
git log -10 = Last 10 commits
git log <file> = Show commits by one file
git log --author="Pardip" = Show commits by one author
git log --since="2024-01-01" --until="2024-06-01" = Show between dates
git commit --allow-empty -m "Start project" = Create an empty commit
git commit --amend --no-edit = update last commit, can't change the message
git commit --amend = it replaces the previous commit with same message
git commit --amend -m "new message" = Update last commit message
git reset --soft HEAD~1 = Undo last commit
git tag <tag-name> = add tag to last commit
git tag -a <tagname> -m "message" = create tag with tagger’s name and email, date, message
git tag <tagname> <commit-hash> = add Tag to specific commit
git tag = Show list of tags
git show <tagname> = Show specific tag details
git push origin <tagname> = Push one specific tag
git push origin --tags = Push all local tags
git tag -d <tagname> = delete tag
git stash = Git stash temporarily saves your uncommitted changes without committing them, and cleans your working directory.
git stash list = This shows all your stashes.
git stash push -m "message" = Stash with a message
git stash apply stash@{n} = Bring back the changes from the specific stash
git stash branch <branchname> = create branch and automatically move your latest stash onto it.
git stash branch new-feature stash@{n} = create branch and move specific stash onto it.
git stash show = See what was changed in the latest stash
git stash apply = restores your last stashed into working directory.
git stash pop = Apply the latest stash and remove it from the stack:
git stash drop stash@{n} = Delete a specific stash
git stash clear = Delete all your stashes at once
git log = Show full commit history
git log --oneline = Show a summary of commits
git show <commit> = Show details of a specific commit
git diff = diff between working directory and the last commit (unstaged changes)
git diff --staged = shows what will be committed
git diff <commit1> <commit2> = See what changed between any two commits
git log --stat = which files were changed in each commit and how many lines were added or removed
git log --graph = graph of your branch history
git help <command> - See the manual page for a command
git <command> --help = See help for a command (same as above)
git <command> -h = See a quick summary of options
git help --all = List all possible Git commands
git help -g = List guides and concepts
git branch new-branch-name = created a new branch
git branch = List all branches in your repository
git checkout branch-name = Switching Between Branches
git branch -d branch-name = delete a branch
git branch -m old-name new-name = Rename a branch
git branch -D branch-name = To force delete an unmerged branch
git merge branch-name = To combine the changes from one branch into another
git merge --abort = Abort a merge







ls = List files in the folder
pwd = Print Working Directory.

ssh-keygen -t ed25519 -C "your_email@example.com" = Create SSH keys
cat ~/.ssh/id_ed25519.pub = it will print your public SSH Key


